
Feutre:

    git branch <name_of_branch>

  you could still work on the main brach if you like
    git branch -m

merge a branch to main:
    git checkout main
    git merge <name_of_branch>
    :q!
    git push origin main -u


see all branches:
    git branch

get into a branch:
    git checkout <name_of_the_branch>

